import styled from 'styled-components';

export const InputSeach = styled.div`
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;

    input {
        width: 50%;
        margin: 1rem 0;
        height: 2rem;
        padding: 1.2rem;
        text-align: left;
        display: flex;
        color: rgba(67, 121, 222, 0.9);
        font-size: 1rem;
        border: 0;
        border-bottom: 0.2rem solid rgba(67, 121, 222, 0.9);
        outline: 0;
        cursor: pointer;
    }
`;
